package de.htw.saarland.gamedev.nap.data;

public class GameLogic {
	
	public void respawn(){
		
		if(Timer.TIME>=0 && Timer.TIME<=60){
			
		}
		if(Timer.TIME>=60 && Timer.TIME<=120){
			
		}
		if(Timer.TIME>=120 && Timer.TIME<=180){
			
		}
		if(Timer.TIME>=180 && Timer.TIME<=240){
			
		}
		if(Timer.TIME>=240 && Timer.TIME<=300){
			
		}
		if(Timer.TIME>=300 && Timer.TIME<=360){
			
		}
	}
}
